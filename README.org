For example, if you want to install personal package ~iotools~, just enter the
folder iotools: 

cd iotools

and execute 

pip3 install -e .

which will install the package with a symlink, so that changes to the soulce
file will be immediately abailable to other users of the package on our system.
